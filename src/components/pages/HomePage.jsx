import { Carousel } from "../carousel/Carousel";
import { Header } from "../header/Header";

export const Homepage = () => {
  return (
    <div>
      <Header />
      <Carousel />
    </div>
  );
};
